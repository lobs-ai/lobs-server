
# Safety: tests must never run against a live production server.
# Always use ASGITransport (in-process). Never set LOBS_API_URL to localhost.
# If you see DB lock errors during tests, you're probably testing against prod.
