Session cleanup fix already implemented: delete_session() in workflow_nodes.py delegates to WorkerGateway.delete_session() which uses WS sessions.delete JSON-RPC (not sessions_kill). workflow_seeds.py cleanup nodes have session_refs for all spawn nodes. 11 targeted tests pass; full suite hangs (pre-existing infra issue unrelated to this task).
