Fixed DB lock on worker_status and worker_runs endpoints. Added retry-with-backoff (5 attempts, 0.5s exponential backoff) to PUT /worker/status and POST /worker/history endpoints. Verified existing retry logic in orchestrator/worker.py, agent_tracker.py, and WAL/busy_timeout config in database.py. Existing test suite passes.
