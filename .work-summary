Fixed database lock issues in worker.py: corrected _record_worker_run call parameters in _handle_escalation method to use proper signature (start_time, duration, exit_code, summary). All 14 DB lock retry tests pass with 5-attempt exponential backoff already in place for worker_status updates and worker_runs inserts. WAL mode and 30s busy_timeout confirmed enabled.
