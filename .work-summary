Fixed DB lock on worker_status and worker_runs operations with retry-with-backoff logic. Added exponential backoff to 5 key commits in worker_monitor.py; verified WAL mode properly configured. All worker tests passing (20/20).
