Task already completed by prior agent (commit 98c48d0). All 67 workflow tests pass. The fix: workflow_nodes.py delete_session() delegates to worker_gateway.delete_session() which uses WebSocket sessions.delete; workflow_seeds.py spawn_agent workflows have session_refs in cleanup nodes. Pre-existing unrelated test failures in 128 other tests unchanged.
