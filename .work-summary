Fixed session cleanup: sessions.delete WebSocket JSON-RPC impl in worker_gateway.py was already in place (prior agent fixed sessions_kill). Verified workflow_nodes.py cleanup logic correct, workflow_seeds.py has session_refs for all spawn nodes. All 11 session cleanup tests pass.
