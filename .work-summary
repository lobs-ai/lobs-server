Fixed DB lock on worker_status and worker_runs with retry-with-backoff. Enhanced database pragma settings (busy_timeout: 30s, synchronous=NORMAL). Added retry logic (5 attempts, exponential backoff) to worker status endpoints. All tests passing.
