Verified DB lock fix is fully implemented: WAL mode enabled, busy_timeout=30000ms (30s), retry-with-exponential-backoff on all critical writes (worker.py, agent_tracker.py, routers). All 49 related tests pass.
