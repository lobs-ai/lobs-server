Session cleanup fix was already applied in prior commits (sessions_kill → sessions.delete via WebSocket in worker_gateway.py; workflow_seeds.py cleanup nodes already have session_refs). Added 4 new tests for cleanup node session_ref/session_refs behavior; 76 workflow tests pass. Pre-existing failure in test_api_contract_swift.py (missing content field in memory API) is unrelated.
