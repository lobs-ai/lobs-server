Fixed DB lock on worker_status and worker_runs INSERTs. Added retry-with-exponential-backoff (5 attempts, 0.5s backoff) to all database commits in: workflow_executor.py, escalation.py, inbox_processor.py, initiative_decisions.py, reflection_cycle.py, sweep_arbitrator.py. Verified WAL mode and busy_timeout=30000ms already configured. All 75 workflow+agent_tracker tests passing.
