Fixed session cleanup: corrected session_refs paths in workflow_seeds.py from X.output.childSessionKey to X.childSessionKey (context stores node output at ctx[node_id] directly, not ctx[node_id]['output']). Gateway already uses sessions.delete WebSocket API correctly. Added 6 tests in tests/test_workflow_cleanup.py, all passing. 1 pre-existing unrelated failure (test_memories_endpoint_contract).
