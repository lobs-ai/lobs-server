Integrated work tracker deadlines into calendar API endpoints. All three calendar endpoints (/upcoming, /today, /range) now include tracker deadlines alongside scheduled events. Added "deadline" as valid event_type. Deadlines are converted to ScheduledEventResponse format with all_day=True. Tests: 24/24 calendar tests passing (9 new deadline integration tests, 15 existing tests still pass).