Session cleanup already fixed in previous runs. Gateway uses sessions.delete WebSocket RPC (not sessions_kill). workflow_nodes.py delegates to gateway.delete_session(). Workflow seeds with spawn_agent nodes have session_refs. All 5 session/cleanup tests pass; 1 pre-existing unrelated test failure (test_memories_endpoint_contract missing 'content' field).
