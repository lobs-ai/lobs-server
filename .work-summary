Fixed session cleanup: NodeHandlers.delete_session() now uses Gateway WebSocket sessions.delete API (not sessions_kill). workflow_seeds.py cleanup nodes include session_refs for all spawn_agent workflows (task-router, code-task, research-task). 78 tests pass.
