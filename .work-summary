Verified DB lock handling: worker.py and worker_manager.py have 5-attempt retry loops with exponential backoff (0.5s increments) for worker_status UPDATE and worker_runs INSERT. agent_tracker.py has identical retry logic. Database configured with WAL mode and busy_timeout=30000ms. All 66 retry-related tests pass.
