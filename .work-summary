Fixed DB lock contention on worker_status and worker_runs by adding retry-with-backoff to all unguarded commits. Updated monitor_enhanced.py with 5 retry attempts and exponential backoff (0, 0.5, 1.0, 1.5, 2.0s) on all 5 db.commit() calls. Verified WAL mode is enabled and busy_timeout set to 10000ms/15000ms. All worker status and monitor enhanced retry tests passing.
