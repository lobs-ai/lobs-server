# Docs moved to ~/lobs-shared-memory

All documentation has been consolidated into the shared memory repo at `~/lobs-shared-memory/docs/`.

This keeps all agent-accessible knowledge in one place, indexed by OpenClaw memory search.
